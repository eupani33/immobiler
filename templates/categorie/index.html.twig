{% extends 'base.html.twig' %}

{% block title "Categories" %}

{% block body %}
	<div class="container mt-5">
		<div class="row">
			<div class="col">
				<table id="datatable" data-toggle="table" data-search="true" class="table table-striped table-bordered" data-pagination="true">
					<thead>
						<tr>
							<th data-field="Famille" data-sortable="true" data-align="center">Famille</th>
							<th data-field="categorie" data-align="center">Catégories</th>
							<th data-field="action" data-align="center">Action</th>
						</tr>
					</thead>

					<tbody>
						{% for categorie in categories %}
                          
							<tr>
								<td>{{ categorie.nom }}</td>
								<td>{{ categorie.categorie }}</td>
								<td><a href="{{ path('categorie_edit', {'id': categorie.id }) }}">edit</a></td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</div>
			<a href="{{ path('categorie_new') }}">Nouvel enregistrement</a>
		</div>
	</div>
{% endblock %}
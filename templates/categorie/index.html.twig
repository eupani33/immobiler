{% extends 'base.html.twig' %}

{% block title "Categories" %}

{% block body %}
	<div class="container mt-5">
		<div class="row">
			<div class="col">
				<table id="datatable" class="table table-striped table-bordered">
					<thead>
						<tr>
							<th data-field="Famille" data-halign="center" data-align="center">Famille</th>
							<th data-field="categorie" data-halign="center" data-align="center">Catégories</th>
							<th data-field="action" data-halign="center" data-align="center">Action</th>
						</tr>
					</thead>

					<tbody>
						{% for categorie in categories %}
							<tr>
								<td>{{ categorie.nom }}
									&nbsp;
								</td>
								<td>{{ categorie.categorie }}&nbsp;</td>
								<td>
									<a href="{{ path('categorie_edit', {'id': categorie.id}) }}">edit</a>
								</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</div>
			<a href="{{ path('categorie_new') }}">Nouvel enregistrement</a>
		</div>
	</div>
{% endblock %}
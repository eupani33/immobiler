{% extends 'base.html.twig' %}

{% block title "Ecritures" %}

{% block body %}
	<div class="container mt-5">
		<div class="row">
			<div class="col">
				<table id="datatable" class="table table-striped table-bordered">
					<thead>
						<tr align="center">
							<th >Date</th>
							<th>Type</th>
							<th>Libelle</th>
							<th>Montant</th>
							<th>Pointage</th>
							<th>actions</th>
						</tr>
					</thead>
					<tbody>
						{% for ecriture in ecritures %}
							<tr>
								<td align="center">{{ ecriture.date ? ecriture.date|date('d/m/Y') : '' }}</td>
								<td align="center">{{ ecriture.type }}</td>
								<td align="right">{{ ecriture.libelle }}</td>
								<td align="right">{{ ecriture.montant_format }}
									€</td>
								<td align="center"> {{ ecriture.pointage }}</td>
								<td align="center">
									<a href="{{ path('ecriture_edit', {'id': ecriture.id}) }}">edit</a>
								</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</div>
		</div>
	</div>
	<a href="{{ path('ecriture_new') }}">Create new</a>
{% endblock %}
{% extends 'base.html.twig' %}
{% block title "Liste Fournisseurs" %}
{% block body %}
	<div class="container mt-5">
		<div class="row">
			<div class="col">
				<table id="datatable" data-toggle="table" data-search="true" class="table table-striped table-bordered" data-pagination="true">
					<thead>

						<th data-field="nom" data-sortable="true" data-align="center">Libelle</th>
						<th data-field="local" data-align="center">Contact</th>
						<th data-field="ville" data-align="center">Ville</th>
						<th data-field="adresse" data-align="center">Adresse</th>
						<th data-field="cp" data-align="center">Code postal</th>
						<th data-field="email" data-align="center">Email</th>
						<th data-field="tel" data-align="center">Tél</th>
						<th data-field="action" data-align="center">Action</th>
					</thead>
				</tr>
			</thead>
			<tbody>
				{% for fournisseur in fournisseurs %}
					<tr>
						<td>{{ fournisseur.nom }}</td>
						<td>{{ fournisseur.contact }}</td>
						<td>{{ fournisseur.adresse }}</td>
						<td>{{ fournisseur.ville }}</td>
						<td>{{ fournisseur.cp }}</td>
						<td>{{ fournisseur.email }}</td>
						<td>{{ fournisseur.tel }}</td>
						<td>
							<a href="{{ path('fournisseur_edit', {'id': fournisseur.id}) }}">edit</a>
						</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>
	<a href="{{ path('fournisseur_new') }}">Nouvel enregistrement</a>
</div></div>{% endblock %}